# Claude Code / dev workflow aliases
# Source from ~/.bashrc or ~/.bash_aliases:
#   if [ -f ~/dev-config/bash/aliases.sh ]; then
#       . ~/dev-config/bash/aliases.sh
#   fi

# Jump into a project and start Claude Code
# Rename / duplicate for each project:
# alias sail='cd ~/sailbook && claude'
# alias myapp='cd ~/myapp && claude'
