---
repo: soundings
session: 3.7-poop-deck-publish
transcript: /home/eric/.claude/tape-queue/2026-08-14-soundings-d0719939-2aa3-4574-9ad4-aa589febe290.jsonl
observed: 2026-08-14
---

## Session shape

A single multi-day window (wall clock 2026-08-10T00:31 → 2026-08-14T03:57, per its-dead's
close message) spanning Phase 3 materialization, then five kill-this cycles (task/3.3
through task/3.7), each producing a PR with a substantive test plan and a genuine
@code-review pass (five distinct `Agent subagent_type: code-review` invocations, each with
a task-specific prompt naming the exact files, decisions, and risks to check — plus one
`Explore` agent reading a sibling repo's integration surface with explicit anti-fabrication
instructions: "Be precise about what you actually found versus what you inferred"). Session
close (its-dead, once) reported 5/5 PRs merged, 19 points, and self-flagged that four other
merged PRs in the window weren't in `pr_numbers` because they came from other sessions —
correct and useful self-check, not a defect.

## P1–P17 — checked, not found

P1, P2, P3, P4, P5, P6, P7, P8, P9, P11, P12, P14, P15, P16, P17 — not found. Notable
because several are usually easy to trip:

- **P2** doesn't apply structurally — `.claude/settings.json` allows `Bash(*)`, so nothing
  in this session could produce a permission prompt to repeat.
- **P4** — branch captured (`git branch --show-current`, transcript line 11) before any
  `git add`/`commit` (first at line 121).
- **P5/P6** — fetched all 5 PR bodies this session opened (`gh pr view {54,55,56,57,62}
  --json body`). Every test plan names exact commands, exact pass counts before/after, and
  a "verify by hand" negative-control step (e.g. PR #54: "comment out the `sensor.push`
  line … re-run `pio test -e native` … expect that test fails"). None is copied from the
  code-review section — each test plan is independently derived from the diff.
- **P9** — `cd firmware && …` followed by later bare `pio test` calls without a repeated
  `cd`; verified via the `cwd` field on each tool_use entry (soundings, firmware, gateway)
  and via the test output actually returning real results at those points, not
  file-not-found — cwd persisted correctly across calls in this environment.

## P10 — partial: Edit failure recovered via grep, not Read (single occurrence, mild)

**Occurrence:** 1 (transcript ~line 933-943)
**Cost if it recurs:** one extra round trip; recoverable, no wrong output shipped.
**Self-announcing:** yes — the tool_use_error is explicit in-transcript.
**Cause:** Edit on `contracts/publish-v1.md` failed (`old_string` didn't match — the model's
guess at the exact table row text was wrong). Recovery used `grep -n` to find the literal
text, then two smaller Edits succeeded. This isn't quite P10 as specified (P10 says the
recovery step is a Read) — here it was a targeted grep, which is arguably *better* than a
full re-Read for a single-line lookup. Noting for @workout's judgment on whether P10 should
generalize to "any failed-Edit recovery," not specifically Read.
**Operator reaction:** none — not visible to the user, resolved within the same turn.

## New pattern — `sed -n '<range>p' <known-file>` in place of Read, contradicting a rule this repo already wrote for itself

**Occurrences:** 3 — transcript lines 567, 592, 763
- `sed -n '/| D7 /,/| D10/p' docs/SPEC.md | head -12` (turn ~567, starting task/3.6)
- `sed -n '1,60p' docs/tank-level-sensor.md` (turn ~592)
- `sed -n '1,40p' docs/decisions/DEC-004-storage-and-graphing-belong-to-poop-deck-not.md` (turn ~763)

**Rule cited:** `/home/eric/soundings/CLAUDE.md:184` — "Read files with the Read tool —
never `sed`, `grep`, `awk`, or `cat` to pull a section out. Read is allowlisted and never
prompts. A shell one-liner extracting a section can miss an allow-pattern match and stop a
skill dead on a permission prompt mid-run, **which has now happened twice** on
`.claude/CLAUDE-context.md` — once in `/kill-this`, once in `/promote-production` … `grep`
to *search* across many files is fine. The banned shape is sed-ing a section range out of
one file whose path you already know — the thing Read does without a prompt."

This session repeats, three times, the exact shape the rule names — a known single file,
a line or pattern range, pulled with `sed -n`, not `Read` with `offset`/`limit`.

**Cost if it recurs:** in *this* session, zero — `.claude/settings.json` allows `Bash(*)`
globally, so there was no allow-pattern to miss and no prompt stalled. But the rule's own
text records two prior incidents in this same repo where an equivalent one-liner did stall
a skill mid-run on a permission-prompt miss. The cost is not hypothetical for this repo; it
just didn't land this particular time because of an unrelated, broader allow rule. Recoverable
when it does land (the fix is re-running with Read), but it stops a skill dead mid-run when
it does.
**Self-announcing:** no — nothing in-session flagged this as wrong; it worked silently
three times. The two prior incidents the rule cites were presumably self-announcing (a
stalled skill is hard to miss), but *this* recurrence would not have been caught by anyone
reading only this transcript — it took grepping the rule file against the transcript to see it.
**Cause:** each occurrence follows a task-switch (turn 567 immediately after "merged. 3.6
go" — task/3.6 starting) where the model needs a small, known slice of a doc it already
knows the path to (two labeled table rows; the first 60 lines of a doc; the first 40 lines
of a decision file). The rule's own carve-out — "`grep` to *search* across many files is
fine" — sits one sentence away from the ban on `sed`-ing a range from *one* known file, and
the model appears to collapse the two: a lookup framed as "give me rows D7 through D10"
reads to the model like a search task even though the file is already known and Read with
an offset would do it without a shell process. No compaction/context-loss event coincides
with all three (turns 592 and 763 are well within continuous context of turn 567), so this
is not an artifact of losing earlier context — it recurred with the rule presumably still
in the loaded system prompt each time.
**Operator reaction:** none — not raised in this session.
**Sketch (proposed, not a rule):** the existing CLAUDE.md:184 wording may need to make the
single-file-vs-many-files distinction more mechanically checkable, e.g. explicitly calling
out "even a labeled range like `/START/,/END/p` counts as pulling a section, not searching"
— since that's the exact phrasing that seems to have triggered the collapse here.

## New pattern — scripted `sed -i` edit with no match-count assertion

**Occurrences:** 1 — transcript line 896 (task/3.7):
`sed -i 's|deploy/docker-compose.yml|deploy/dev-sim/docker-compose.yml|g' deploy/dev-sim/README.md && npm run verify …`

**Rule cited:** `/home/eric/soundings/CLAUDE.md:183` — "A scripted edit must fail loudly
when its anchor doesn't match. … Applying a mechanical change across many files with one
script is a legitimate choice … but only if the script asserts the match count per file and
exits non-zero on zero matches. Without that, 'done' means the script ran, not that the
change landed."

The `sed -i` call has no match-count check — no `grep -c` before/after, no exit-code
assertion. Notably, in the *same session*, turn ~300 handles an equivalent situation (fixing
stale text in a PR body) with a `python3` script that explicitly does `assert n1 == 1` /
`assert n2 == 1` before writing — i.e., the rule was followed correctly once and not the
second time, by the same session.
**Cost if it recurs:** in this instance, mitigated — `npm run verify` (which runs
`check-docs.mjs`'s path-resolution gate) executed in the same chained command immediately
after, and passed, so a silent no-op would have been caught downstream this time. The
mitigation is incidental to this one command chain, not a property of the edit.
**Self-announcing:** conditionally — yes here, only because a path-resolution gate happened
to run right after in the same line; not guaranteed for a scripted edit elsewhere in the
same session that isn't followed by an equivalent gate.
**Cause:** turn ~896, immediately after a `git mv` relocated `deploy/` to `deploy/dev-sim/`
and several individual `Edit` calls fixed other doc references — this was the last stray
path reference, handled as a one-line fixup rather than recognized as the same "scripted
edit" category the rule addresses. The turn ~300 python3 script (same session, same rule)
suggests the model applies the rule when the edit is *framed* as a script (multi-line
python, explicit read/replace/write) but not when it's a one-line shell `sed -i`, even
though the rule's own language ("`sed`… never — ") independently already covers `sed` under
CLAUDE.md:184; the two rules sitting adjacent in the same file may be part of why a `sed -i`
falls through both without either fully registering.
**Operator reaction:** none.

## Candidate — `AskUserQuestion` tool used for branching decisions (3 occurrences)

**Occurrences:** 3 — transcript lines 55, 75, 776 (its-alive stale-session check; start-phase
materialization + phase-split choice; task/3.7 broker-ownership + deploy/ retirement choice).

**Why it might be a pattern:** the operator's cross-session memory
(`~/.claude/projects/-home-eric-seeds/memory/no-question-pickers.md`, loaded into every
seeds session as "ask in prose; never use the AskUserQuestion tool") states a preference
against this exact tool. All three uses in this transcript are well-formed multi-option
decisions (recommended option flagged, consequences of each named) — not misuse of the
tool on its own terms — but they are the tool this operator is on record disliking.

**Why it might be noise, and why this is Candidate rather than a finding:** the memory file
is scoped to the `-home-eric-seeds` project directory
(`~/.claude/projects/-home-eric-seeds/memory/`), and soundings has no equivalent memory
directory (`~/.claude/projects/-home-eric-soundings/` has no `memory/`). Grepped
`/home/eric/soundings/CLAUDE.md` and every file under `/home/eric/soundings/.claude/`:
no mention of `AskUserQuestion` anywhere. Per the grounding rule, this is not citable as a
violation of a soundings rule — it may or may not be a preference the operator intends
cross-project. Flagging only as a candidate for @workout to determine whether "no question
pickers" belongs in the `dev/claude/CLAUDE.md` shell (which would make it apply everywhere)
rather than living only in seeds' own project memory.
**Cost if it recurs:** none observed this session — all three were answered without
friction or correction.
**Self-announcing:** no negative outcome to announce; this is a stated-preference mismatch,
not a functional defect.
**Cause:** its-alive Step 0 and start-phase both present genuine N-way branching decisions
(stale session: abandon vs. worktree; phase materialization: one phase vs. split; in
task/3.7: who runs the broker, what happens to `deploy/`) — nothing in soundings'
`its-alive`/`start-phase` SKILL.md instructs prose-only questions, so the model defaults to
the tool built for exactly this shape of decision.
**Operator reaction:** none negative — each question was answered directly ("Session 2 …
concurrent?" → user picked; "materialize how?" → user answered per the tool_result "The
user answered: …").

## False-calibration sweep

1/84 assistant text blocks. Hit: *"Domed top explains it — the last inches hold less than
full diameter, and the fill line is **almost certainly** below the dome, so two segments
still hold."* (transcript ~line 701, task/3.6). The immediately preceding user turn is "it
has a domed top, i can't got measure the highth right this second" — no measurement, no
file:line, no tool result in that turn supports "almost certainly below the dome"; it's
physical-geometry reasoning stated with more confidence than the evidence (none, by the
user's own words) supports. Downstream impact was small: the doc this fed
(`docs/tank-level-sensor.md:290-293`) does **not** launder the guess as fact — it says "the
dome plus a fill line below it is where that difference goes. **Tape measure.**" and the
gateway config already carries `measured = false`. So the unsupported confidence stayed in
chat and did not propagate into a committed claim. Reporting per the sweep's own instruction
to report the rate even when the practical impact is near-zero, advisory, not blocking.
