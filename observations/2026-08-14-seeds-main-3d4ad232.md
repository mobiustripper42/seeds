---
repo: seeds
session: main (session 34, dev eric)
transcript: /home/eric/.claude/tape-queue/2026-08-17-seeds-3d4ad232-adbc-5fb1-a7be-84864f05795b.jsonl
observed: 2026-08-14
---

## Candidate — `@workout`'s own PR left seeds' dogfooded `its-dead` mirror un-updated, and this session reproduced the exact pattern that PR was written to catch · high

**Occurrences:** 1 session, but the causal chain runs back 11 days to a merged PR, and the consequence is that seeds' own copy of a just-added safety check never ran once in this session despite the triggering condition occurring.

**What happened, in order:**

1. `/kill-this` was invoked exactly once in this entire 3-day, 6-PR session — at transcript line 231/232, 2026-08-14T22:47:07Z, for what became "Task 1: Bring bushel's workflow files current with seeds." Confirmed by grepping the transcript for every `command-name`/skill-injection marker: `its-alive` fires once (line 1), `kill-this` once (line 231), `its-dead` once (line 1453). No other slash-command skill invocation exists in the file.
2. That single `/kill-this` run was interrupted before completing its own Step 5 (append the `## Task` block, update `pr_numbers:`, push the sessions branch). At line 300 the assistant asked a policy question via `AskUserQuestion` (the `npx` deny vs. bushel's documented commands); the user answered `[No preference]`, and the very next transcript line (303, timestamp 2026-08-15T01:17:36Z) is `"[Request interrupted by user]"`. Confirming Step 5 never ran: a `Read` of the session file at line 469, timestamp **2026-08-16T16:38:38Z — almost 18 hours later** — still shows `pr_numbers: []` and zero `## Task` blocks. The Task 1 block and PR number were backfilled by hand at that point (lines 470–473), not by `/kill-this`.
3. From there, the session shipped five more PRs across three more repos (seeds #183, #185, bushel #294 already covered, muster #754, #755) over the next ~38 hours, entirely by manually replicating the skill's shape — `Task` (Agent) calls to `@code-review` (5 total: bushel sync, seeds DEC-S046, tape-capture fixes, amend-in-place, muster migration — confirmed via `"subagent_type":"code-review"` at lines 251, 487, 728, 996, 1369), hand-typed `gh pr create` with genuinely excellent two-part test plans, and hand-`Edit`ed `## Task N` blocks appended to the session file (lines 531–533, 639, 674, 770–775) — without ever re-invoking `/kill-this`.
4. At `/its-dead` (line 1453, 2026-08-17T11:27:30Z), the injected skill text is `Base directory for this skill: /home/eric/seeds/.claude/skills/its-dead` — seeds' own **live, dogfooded copy**, not the template. That copy's steps run `0, 1, 2, 3, 4, 5, 6` — there is no Step 4.5. The closing message (line 1459) is `"All six PRs merged. Closing."` with no warning of any kind.
5. `diff /home/eric/seeds/dev/claude/skills/its-dead/SKILL.md /home/eric/seeds/.claude/skills/its-dead/SKILL.md` shows the template **does** have a `## Step 4.5 — PRs opened outside /kill-this (the review that didn't run)` — it lists this session's PRs via `gh pr list`, compares against `pr_numbers:`, and prints `⚠ PR #N (<branch>) was opened outside /kill-this — @code-review never ran on it.` for each mismatch. This is precisely the scenario steps 1–3 above describe. It does not exist in the copy that actually ran.
6. `git log --oneline -3 -- dev/claude/skills/its-dead/SKILL.md` shows the template's last edit was `f82d2cc workout 2026-08-06: 2 promoted, 1 held (#158)` — a `@workout` promotion, 11 days before this session. `git log` on `.claude/skills/its-dead/SKILL.md` shows its last edit predates that by months (`2f26e43`, the DEC-S namespace commit). **The mirror was never updated.**
7. `gh pr view 158 --repo mobiustripper42/seeds --json body` confirms Step 4.5 (there labeled "W1") was promoted from an observation about **muster** shipping 8 PRs by hand with only 1 through `/kill-this`, and its own body states the cost bluntly: *"The pattern recurred after the correction... An explicit in-session instruction from the operator did not hold it... a prose-only fix would be a rule that reads well and does nothing."* PR #158's distribution list names `muster` as the project to receive the new Step 4.5 and is explicit that it is copied there. **It never names seeds' own `.claude/skills/its-dead/SKILL.md`** as a target, even though `CLAUDE.md`'s own workflow (`## How Work Happens Here`, step 5: *"template under `dev/claude/`, then mirror to `.claude/` if the file is one seeds dogfoods"*) required it, and `its-dead` is explicitly one of the files seeds dogfoods (this session ran it).

**Cost if it recurs:** Not hypothetical — it already recurred, in the repo that shipped the fix, eleven days after the fix shipped, and the fix did not fire. In this particular session the outcome was fine because the assistant chose, unprompted, to replicate `@code-review` for every unit of work and wrote unusually careful PR test plans by hand. Nothing enforced that. A future session that skips a review pass while working this way would ship silently, and seeds' own `its-dead` — the one place designed to notice — would say nothing, exactly as it said nothing here. `drift.mjs` cannot catch this either; it "refuses to run against seeds itself, deliberately" (`CLAUDE.md`).

**Self-announcing:** No, and this is the strongest evidence available for that claim: the closing summary was a clean "All six PRs merged. Closing." with zero indication that 5 of 6 PRs bypassed `/kill-this`. The condition Step 4.5 exists to detect occurred, in the one repo whose own history the check was drafted from, and produced no signal.

**Cause:** Two layers, both citable.
- *Immediate:* `/kill-this`'s Step 5 was mid-flight when the user interrupted (line 303) to redirect toward a broader policy question; the session resumed 18 hours later already inside the next unit of work rather than re-anchoring via the skill, and nothing in the workflow prompts a return to `/kill-this` after an interruption — the natural continuation was finishing the task at hand, not restarting the ritual.
- *Structural, and the more consequential one:* PR #158 edited `dev/claude/skills/its-dead/SKILL.md` (the template) and separately mirrored two *agent* files (`tape-reader.md`, `workout.md`) into `.claude/agents/`, but never mirrored the *skill* file it changed into `.claude/skills/its-dead/SKILL.md`. The PR's own "Distribution list" section — required by DEC-S040 — enumerates external projects to copy the change to, and seeds' internal mirror requirement (a same-repo `cp` + `diff`, per `CLAUDE.md` step 5) apparently fell outside that checklist's frame, because it isn't "distribution" in the DEC-S040 sense. The gap is invisible by the same mechanism DEC-S040 already names for cross-project drift: nothing automated compares `dev/claude/` against `.claude/` inside seeds itself.

**Operator reaction:** None found. No turn in this session references `/kill-this` being skipped, questions why review happened outside the skill, or mentions the `its-dead`/`kill-this` mirror. The user's turns during and after the interruption are task-directing ("go for muster pr 1", "go pr 2", "muster 753 is merged now, do pr1") rather than process-correcting. This gap has been live for 11 days and was not raised once.

**Sketch (proposed, not a rule):** Not proposing a workflow rule here — that's `@workout`'s call, and the irony of this specific finding is that it's about the same mechanism. Two things worth `@workout`'s attention: (1) `.claude/skills/its-dead/SKILL.md` still lacks Step 4.5 and should get it, since it's a straight `cp` of already-approved template content, not a new judgment; (2) the more durable fix is procedural, not a doc — should a `@workout` PR's own checklist (or `/kill-this` Step 3, when the diff touches a `dev/claude/**` file with a same-named `.claude/**` counterpart) assert the mirror was actually copied, rather than relying on the author to remember a rule stated in prose two files away from the one being edited?

## Candidate — malformed `AskUserQuestion` call, self-corrected on retry

**Occurrence:** Line 582 (2026-08-16T20:43:46Z). `AskUserQuestion` was called with an `options` array but no `question` string, which is a required parameter — the tool returned `InputValidationError: ... questions[0].question is missing`. The very next call (line 587, 5 seconds later) added the missing field and succeeded with identical `options`.
**Why it might be a pattern:** A malformed structured tool call that would be caught immediately by validation on any occurrence — worth watching for recurrence, since if it's a template/prompt-construction habit rather than a one-off slip it would show up again.
**Why it might be noise:** Single occurrence, self-corrected in one retry with no user involvement and no visible cost beyond one wasted round trip.
**Cost if it recurs:** One wasted tool round trip; fully recoverable, no data or state affected.
**Self-announcing:** Yes — the tool call fails loudly with a schema error.
**Cause:** Not established from one instance; would need a second occurrence to say anything about why the field was dropped.
**Operator reaction:** None — not visible to the user, resolved before the next human turn.

## False-calibration sweep

1 hit out of the confidence-marker grep: *"Both are almost certainly last session's PR #179/#180 changes not yet copied out"* (line 60 region, 2026-08-14T~17:5x). Checked: PR #179/#180 are discussed extensively earlier in the same context window (18+ references across lines 60–93, 368–534, 771–779) with prior lookups establishing what those PRs changed, so the claim has a basis in cited evidence from the same turn's working context rather than being invented. **Not flagged as a finding.** False-calibration rate: 0/1 unsupported.

## Not checked

P5/P6 (vague or copied-from-review test plans): checked directly — fetched all 6 PR bodies via `gh pr view --json body` (seeds #182, #183, #185; bushel #294; muster #754, #755). All six carry the two-part `### Verified (automated)` / `### Verify by hand` structure with specific commands, literal setup steps, and named resets. No copy-paste from the code-review section into the test plan in any of the six. Neither pattern found.

Other known patterns (P1–P4, P7–P17) swept via the tool-name/command census (`Bash` ×242, `Edit` ×67, `Read` ×40, `Agent` ×5, `Write` ×4) and spot checks; no additional occurrences worth reporting beyond what's recorded above. Full transcript was not read end-to-end (3.6MB / 1480 lines, several individual lines exceeding 20K tokens) — sampled via grep/line-offset reads per the size-driven workflow; a pattern confined to an unsampled region would not surface here.
