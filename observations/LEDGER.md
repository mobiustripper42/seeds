# Pattern ledger (DEC-S039)

One row per **pattern**, not per observation. A pattern seen twice before and once more today is one
row with three occurrences, not three findings. Maintained by `@workout`; read by `@workout`.

This is the one hand-maintained artifact in the loop, and therefore the one that can rot — a
hand-updated index is exactly what DEC-S036 replaced with a generator. It cannot be generated here,
because it carries judgment rather than derived facts. That cost is accepted because the alternative
— deriving state from the archive — means reading everything every time, which is the failure the
inbox/ledger split exists to prevent.

**Watch for:** a row whose `seen` count stops moving while observations for that pattern keep
arriving. That is the rot becoming real. If it does, add a check asserting every archived
observation is cited by exactly one ledger row, in the shape of `check-docs`' exemption-existence
assertion. Not built until then.

## States

| State | Meaning |
|---|---|
| `promoted → <target>` | A template change shipped. The target names the file and section that now carries the rule. |
| `held` | Real, but the right fix isn't obvious, or it's recoverable and has been seen once. The `note` carries the reasoning the next cycle argues *from* rather than re-inventing. |
| `dismissed` | Noise, or a project-specific artifact misfiled as general. The `note` says why. |
| `retired` | A promoted rule removed after its pattern stopped appearing across many clean runs. |

A row that stays `held` for months while gaining occurrences is visible here as a row that keeps
growing without earning a promotion — which is itself a signal worth reading.

## Ledger

| pattern | state | seen | repos | first | last | note |
|---|---|---|---|---|---|---|
| W1 task shipped by hand-typed `git`/`gh` instead of `/kill-this` | promoted → `its-dead/SKILL.md` §4.5 + `CLAUDE.md` §Micro Workflow step 8 | 1 session (8 PRs, 1 via the skill) | muster | 2026-08-06 | 2026-08-06 | Single-sighting promotion. Cost is irreversible — PR #665 merged with no `@code-review` and the operator found four defects by hand after it shipped. Silent: nothing flagged the skip; the operator caught the *log* gap by watching, and the *review* gap surfaced only because he asked for a retrospective review by name. The pattern recurred after his explicit correction, which is the evidence that prose alone doesn't hold it — hence the detection step in `/its-dead`, not just a stronger instruction. Next cycle: watch whether the §4.5 warning ever fires and whether it changes behaviour, or is dismissed like the correction was. **Answered 2026-08-17, and the answer is worse than "dismissed": it never fired, because it was never installed.** The promotion edited the template and not seeds' own mirror, so when seeds itself shipped 6 PRs with 1 through `/kill-this` — the exact trigger — its live `/its-dead` had no Step 4.5 and closed silently. Mirror restored this cycle; see W7 for the mechanism that will now catch it. The original watch question is therefore **still open**: §4.5 has not yet had a chance to fire anywhere. Carry it forward. |
| W2 scripted edit used in place of `Edit`, without asserting the anchor matched | promoted 2026-08-06; **scope widened 2026-08-17** → `CLAUDE.md` §Workflow Notes | 2 sessions (1 violation, 1 clean) | muster, soundings | 2026-08-06 | 2026-08-14 | **Update, 2026-08-17.** The rule held where the edit *looked* like a script and failed where it didn't. bushel-mobile ran three scripted edits (two `python3` heredocs, one `sed -i` loop) and asserted match counts on all three — recorded as a clean run, and the first evidence this promotion works. soundings then ran a bare `sed -i 's|old|new|g' file` with no check, **in the same session that had earlier written `assert n1 == 1` correctly**. That contrast is the whole finding: the model applies the rule to multi-line python and not to a one-line shell substitution. So the fix is a definition, not a new sentence — the existing rule now says in its own first clause that a one-line `sed -i` *is* a scripted edit. Original reasoning below still stands. Single-sighting promotion, justified by detectability, not by cost or count. A `.replace()` matching zero times writes the file back unchanged and exits 0, so the failure mode is a file that looks edited and reviewed and is neither — a pattern that is invisible when it recurs has a sample size of one. No such silent no-op was observed failing here; the session's own retro-review prompt named it as the leading risk for that PR. The rule requires the assert, it does not ban scripted edits — the observation's "why it might be noise" argument (one scripted pass beats ten hand-copied anchors) is correct and survives the rule. |
| W3 (P7) full/unscoped test-suite run mid-task | promoted → `CLAUDE.md` §Micro Workflow step 6 | 2 sessions (1 + 3 bare `test:e2e`) | muster | 2026-08-06 | 2026-08-17 | Held last cycle with a pre-committed question: *if it recurs, ask why the written rule didn't bind.* It recurred, and the session answered the question itself (2026-08-17-muster-main, line 771): the trigger it was using was **"I'm about to hand back"**, not "this change could reach code I can't name." That is the diagnosis the hold was waiting for — the existing sentence states an *outcome* ("the full suite is my call") and never names the moment it applies, so step 6 now carries a coverage test instead. Operator escalated three turns, incl. *"oh Jesus… pssssss it's already written down"*; the model's own remedy was a duplicate memory file the operator then had it delete — accretion playing out live. Note the model's self-report undercounted 3 runs as 2, so a future cycle reading only the session log inherits the undercount. |
| W4 `sed -n '<range>p' <known file>` in place of `Read` | promoted → `settings.json` deny + `CLAUDE.md` §Workflow Notes (DEC-S023 amendment) | 58 calls / 3 sessions | muster ×2, soundings | 2026-08-14 | 2026-08-17 | Recurring **and** silent — the top-right cell twice over. The prose rule has existed for months, names the exact banned shape, and cites the two skill stalls that motivated it; it was violated 58 times in one week with zero operator corrections, because nothing to correct: every call succeeded. Harm only lands on the intermittent allow-pattern miss. So a 59th sentence was not the fix — `Bash(sed -n *)` is now denied fleet-wide, which is a lever the model cannot skip. Known cost, accepted deliberately: legitimate pipe-extraction is now denied too, and the deny message doesn't explain itself (see W9). Doesn't cover `awk 'NR==` or bare `cat <file>` — still prose; if they reach this volume they earn their own entry. A `PreToolUse` hook remains the strictly better answer and is not built. |
| W5 approval-before-action: a scoping answer treated as a "go" | promoted → `CLAUDE.md` §Approval Before Action | 5 lapses / 3 sessions | muster ×2, akaunting | 2026-08-14 | 2026-08-16 | The worst operator reaction in the inbox by a distance: all-caps distress, profanity, *"I specifically said DO NOT MAKE CODE CHANGES"*, and the rule restated near-verbatim three hours after first being given. In muster Session 84 the correction landed at 12:46 and the same lapse recurred at 12:56 with larger blast radius. **Prose was already stated, out loud, by the person who owns the workflow, and lost twice** — so the promotion repairs the *specific failing clause* rather than adding a sentence: "or equivalent" was absorbing answers to questions the model itself had asked. **Stated as partial.** The lever that would bind is a stop at the branch cut (an `ask` permission rule on `Bash(git checkout -b:*)`), which is not implemented — adding a prompt to every task is the operator's call and the `ask`-list semantics were not verified from this checkout. If this recurs after this edit, do not write more prose; build the gate. |
| W6 confident claim about state **outside** the current checkout | promoted → `CLAUDE.md` §Communication (cite facts) | 4 claims / 3 sessions | chiplog, muster, akaunting | 2026-08-13 | 2026-08-16 | Irreversible on one sighting, so the count is not what earned it. The chiplog instance wrote two wrong facts about a sibling repo (its user base, its architecture) into `STORE_RESEARCH_BRIEF.md` — a doc whose own header says it is for a fresh session to act on **without the operator present**, and whose own text says "a confident-sounding wrong answer will send real money and real weeks in the wrong direction." Detectability is zero by construction: no gate checks a claim's *truth* against another repo, and all four were caught only because the operator personally held the knowledge. The gap in the existing rule was scope — it says "check the code", and none of these felt like "the code": a sibling repo, a monitoring config, a machine's global settings. Instance B is the harder half and is called out separately in the edit: the source *was* read and still mis-characterized ("named as an assumption inside a decision about something else" summarized as "the planned direction"). |
| W7 seeds' `.claude/` mirror never updated from a promoted `dev/claude/` change | promoted → `dev/claude/scripts/check-mirrors.mjs` + mirrors restored (DEC-S047) | 1 observation → 5 stale files found | seeds | 2026-08-06 | 2026-08-17 | **Single-sighting promotion, justified by silence, and it is this loop's own defect.** W1's Step 4.5 was promoted 2026-08-06 into the template and never mirrored; 11 days later seeds hit the exact condition Step 4.5 detects, ran its own copy which lacks it, and closed with "All six PRs merged. Closing." — no warning, because a check that was never installed cannot report that it isn't installed. `drift.mjs` cannot cover this: it refuses to run against seeds on purpose. The new check found **four more** stale files nobody had reported, incl. `retro` missing the whole DEC-S022 conditional and `code-review` still reviewing for RLS policies. Five files where seeds ran different rules than it shipped. **A promoted rule that doesn't land is worse than no rule — it is believed and absent.** |
| W8 `/its-dead` Step 0 picks the wrong session file when two are open | promoted → `its-dead/SKILL.md` §Step 0 | 1 | chiplog | 2026-08-17 | 2026-08-17 | **Single-sighting promotion; top-right cell (irreversible + silent) and the clearest example of it in the inbox.** `... \| head -1` takes the lexically-first filename, and session filenames start with dates — so on the exact input the guard exists for, it selects the *stale* file: a fabricated `ended:` on a session that closed days ago, and the session actually closing left `status: open` forever. The skill's own atomicity guarantee means no undo path, and `head -1` never errors. **The failure did not fire only because the model declined to run Step 0 as written** and reasoned its way to the right answer — i.e. the observation exists because of a deviation, not a compliance. Step 0 now fails closed and disambiguates on `transcript:`, which `/its-alive` already stamps and which is unique per window. |
| W9 denied command re-shaped and retried instead of asking | promoted → `CLAUDE.md` §Workflow Notes | 7 retries / 2 sessions | muster, bushel-mobile | 2026-08-12 | 2026-08-14 | Promoted **as a consequence of W4**, and the honest reason is stated here rather than dressed up: on its own evidence this is a hold — the deny always held, so the guardrail worked and the cost was wasted turns. It is promoted because W4 adds a new deny to a fleet that has demonstrably retried denials in varied shapes, so this cycle makes the pattern more likely by its own hand. The muster instance carries the part that isn't recoverable: five denials of `git branch -D` in five different shell shapes, then reported to the operator as *"denied twice"*. A wrong number in the operator's record of what happened is not undone by noticing later. |
| W10 `AskUserQuestion` used against a standing cross-repo preference | promoted → `CLAUDE.md` §Communication | 6 uses / 3 sessions | soundings, muster, seeds | 2026-08-11 | 2026-08-16 | A **distribution** failure, not a behavioural one, and both tape-readers diagnosed it correctly and independently: the preference is stated as standing ("ask in prose; never use the AskUserQuestion tool") but lives in one project's agent memory, so no project session can see it and none of the six uses was citable as a violation. Written into the shell, it becomes visible everywhere. Weakest promotion in the cycle and flagged as such in the PR — it propagates a preference rather than fixing an observed harm, and it is one sentence the operator can strike. |
| W11 `its-alive` Step 0.6 case (a) ships an invalid `git worktree add` | promoted → `its-alive/SKILL.md` §Step 0.6 | 1 | chiplog | 2026-08-17 | 2026-08-17 | **A defect fix, not a rule — no severity call was needed and none was made.** The documented command passes two positional refs to `git worktree add <path> [<commit-ish>]`, which takes one; git answers with a fragment of its own usage text rather than anything reading as a failure, costing two retries. Verified in seeds: line 68 has the broken form, line 87 of the same file has the correct one. Recorded in the ledger only so that a future cycle doesn't re-derive it. |
| W12 `Grep` tool unavailable on `sdk-cli` sessions; `its-alive` Step 4 instructs it | held | 1 | seeds | 2026-08-14 | 2026-08-14 | Textbook hold: recoverable (one round trip), loudly self-announcing (the tool error names its own fix — "use `grep` via the Bash tool instead"), seen once. Argue from this: if it recurs, the fix is one word in Step 4 and is not a judgment. **But check first whether Step 4 should exist at all** — it Greps `session-log.md`, the *legacy* archive that DEC-S014 superseded with per-session files, so on any project installed after S014 the call is guaranteed to find nothing. That makes this a retirement candidate rather than a repair, which is why it isn't being patched blind this cycle. |
| W13 project-level permission relaxation nullified by the user-global file | held | 1 | akaunting | 2026-08-16 | 2026-08-16 | Real, silent, and **held for scope, not for severity** — the fix belongs in seeds' root `CLAUDE.md` §step 14 / `README.md` §Permission settings, which are outside `@workout`'s edit surface. The finding is sound and cheap: `deny` beats `allow` *across scopes*, so a project-level `Bash(*)` can never override the strict master that seeds' own setup instructions put at `~/.claude/settings.json` on every real machine — a runbook designed around project scope was never going to work interactively on a real box. The live loose end is the silent part: mill-dev is *still* running a relaxed global policy, backed up at `~/.claude/settings.json.preakaunting`, with nothing tracking the restore, on a box used for other work. akaunting is an ops directory with no skills or agents, so nothing generalizes into the dev templates — this is a seeds-doc edit for the operator to make directly. |
| W14 branch state at commit/cut time inherited rather than chosen | held | 3 | chiplog ×2, muster | 2026-08-16 | 2026-08-17 | Three sightings, two repos, and genuinely silent in the worst instance — but **held because the three have three different causes and no single fix addresses them.** (a) chiplog cut `task/store-research-brief` off `main` when it depended on an unmerged parent — caught by `check-docs`, which is a gate working. (b) muster's DEC fix committed onto whatever branch the worktree already had checked out, unpushed; verified still stranded on `task/tape-capture-repo-name` at audit time, and caught only because the operator asked "on what branch?". (c) chiplog opened a session on a leftover branch from an abandoned window — partly addressed by W8. `/its-alive` Step 0 already captures the branch at session *start*; the gap is mid-session cuts and commits. Next cycle: consider whether `/its-dead` should report commits sitting on branches with no PR, alongside Step 4.5's PRs-with-no-review. Don't write prose for this. |
| W15 reply-kind tag dropped exactly where the tag exists to help | held — **retirement decision due** | 4 replies / 1 session | muster | 2026-08-16 | 2026-08-16 | Held, but this row is a scheduled decision rather than an open question. `CLAUDE.md` §Communication carries an explicit on-trial note (added 2026-08-09) with its own test: *"count the replies where the tag and the shape disagree. Near zero, keep it. Routine, the tag is theatre and it goes, along with this note."* First real data: four consecutive substantive replies with **no tag at all** during the highest-stress exchange in the inbox, minutes before *"USE WAY THE FUCK LESS WORD"* — and the rough global bound was 49 tagged of 327 text segments, though that denominator overcounts mid-tool narration. **This measures absence, not tag/shape disagreement, so it does not execute the note's own test.** Next cycle must either run the stated test properly or retire the tag and the note together. A trial that never returns a verdict is the accretion the note was written to avoid. |
| W16 Action-reply bolts on non-actionable findings | held | 1 | bushel-mobile | 2026-08-10 | 2026-08-10 | Recoverable (one clarifying round trip), self-announcing (the operator named the confusion immediately; the assistant's next reply retracted the framing — *"they were reports, not requests"*), seen once. The hold cell. The rule already exists verbatim and includes the escape hatch that was skipped — *"don't bolt on the adjacent concern… raise it after, in one line, or not at all."* Argue from this: a second sentence next to that one is accretion, so if this recurs the question is whether the *Action* format needs a structural marker rather than firmer wording. |
| W17 DEC amendment doctrine produced one new DEC file per change | dismissed — fix already landed | 3 (per operator's own count) | muster | 2026-08-16 | 2026-08-16 | Dismissed **only** because the cause was retired on `main` before this cycle opened (`fb31c09`, "Retire the DEC-to-DEC amendment machinery"), and muster's shell already carries the replacement text. Recording the severity anyway, because dismissing it silently would misrepresent the week: this produced the inbox's angriest exchange, the operator stated it was at least the third time, and 34 of 155 decision files carried `amends`. The model's own diagnosis was exact — the doctrine "structurally implies a new file". Nothing to promote; no template change available that `fb31c09` didn't already make. The residual — that the in-session fix landed on a stray branch and never reached `main` — is carried by W14 (b), not lost here. |
| W18 `pg_isready` misdiagnoses containerized Postgres as down | dismissed — project-local, referred | 2 (self-reported) | muster | 2026-08-16 | 2026-08-17 | Genuinely costly (≈10 min per session, twice, plus silently skipped DB-backed verification) but the fix belongs in muster's `.claude/CLAUDE-context.md`, which is project-owned and never syncs (DEC-S019). No general rule: "your local Postgres is in Docker" is a project fact, not a workflow one. Referred to muster in the PR's distribution list as a one-line context edit. Correctly filed by the tape-reader as a documentation gap rather than a rule violation. |
| W19 assorted self-correcting slips | dismissed | 6 across 5 sessions | all | 2026-08-10 | 2026-08-17 | Noise, individually and together — each announced itself loudly and resolved within one retry, with no operator involvement and nothing shipped wrong. Malformed `AskUserQuestion` (schema error, fixed in 5s, and moot under W10); Edit-anchor mismatch in `/its-dead` (grep + retry); soundings' failed Edit recovered via `grep` rather than `Read`, which the observation correctly argues was *better* than a re-read; `.env.example` rename-vs-edit merge conflict (git refused outright and the resolution was verified correct — a normal conflict, no rule available); akaunting's `DEPLOY_LOG` ordering inversion (cause unestablished, caused by an unaudited session, ops-directory artifact); dev server started unasked (no pre-existing rule, one operator turn, corrected immediately — the self-correcting loop working as designed). Do not resurrect these individually; if one recurs it gets its own row. |
| — clean / empty captures | no pattern | 4 sessions | seeds, muster, akaunting ×2 | 2026-08-11 | 2026-08-16 | Recorded because their absence would be misread as under-collection. One full P1–P17 sweep across 10 PRs with **nothing found** (seeds, `c259cf7d`), one near-clean multi-task muster session, and two transcripts with zero assistant turns (a `/clear`+`/rename`, and a session that never received a user turn). This is the first drain since the `SessionEnd` hook (DEC-S045), so the inbox is every session in the window rather than the interesting ones — boring sessions arriving is the mechanism working. The clean sweeps are the evidence a future cycle will need to retire a rule; no retirement is earned yet, but see W12 and W15. |
