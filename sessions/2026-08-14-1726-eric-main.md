---
session: 34
dev: eric
slug: main
branch: main
started: 2026-08-14T17:26:45Z
ended:
points:
pr_numbers: ["bushel#294", 182, 183]
status: open
transcript: /home/eric/.claude/projects/-home-eric-seeds/3d4ad232-adbc-5fb1-a7be-84864f05795b.jsonl
---

# Session 34 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Bring bushel's workflow files current with seeds

**Cross-repo task.** The session is anchored in seeds; the code and the PR are in **bushel**. `pr_numbers` carries a `bushel#` prefix for that reason — `/retro` reading it as a seeds PR number would query the wrong repo.

**Completed:**
- Synced 11 `logic`-class files bushel was behind on, plus the `CLAUDE.md` shell. Nothing in bushel was ahead of seeds, so the copy was one-way — checked per file rather than assumed.
- Deleted three surfaces retired by DEC-S040: `.claude/agents/sync-config.md`, `.claude/skills/pull-seeds/`, `.claude/skills/push-seeds/`.
- **bushel's stale shell contradicted its own context file** — the shell claimed Playwright + pgTAP + `supabase test db`; `.claude/CLAUDE-context.md` says Playwright + vitest + pg-integration (DEC-051) against Neon. The live content already lived in the context file in its newer form, so the verbatim shell copy removed a contradiction rather than losing anything.
- Replaced `.claude/settings.json` with the DEC-S023 master (44 → 102 rules), picking up the DEC-S043 blanket secret deny. Verified non-weakening by hand: every old deny entry is in the new set.
- Rewrote `## Workflow Overrides` as `## Workflow Mechanisms` with the three DEC-S042 slots filled, and dropped the step *numbers* it used to cite.
- Added a `test:e2e` npm script and moved every documented `npx` invocation to `npm run` form.
- Cleaned live retired-skill references out of `docs/AGENTS.md`, `CHEATSHEET.md`, `PROJECT_PLAN.md`, `BUSHEL-MOBILE-KICKOFF.md`, `RUNBOOK-neon-cutover.md`.

**Code review:** 5 findings, all confirmed by hand before fixing, all fixed. The important one was **`doc-consistency.md` — never copied, still routing findings to the deleted `@sync-config`, and reported clean by the differ** because it had no file-class entry and `drift.mjs` skipped what it could not classify. That became DEC-S046 (Task 2). Also: the copied shell cited a `§ Workflow Mechanisms` section that did not exist and no gate could see it; `npx` is denied fleet-wide with `deny` beating `allow`, so bushel's documented E2E and prod-migration commands could not run; two more docs carried live retired-skill instructions; and `scripts/throughput.py` was copied in because the differ said it was missing, when it should never be in a project at all.

**Review passes:** `@code-review` ✓ (5 findings). `/security-review` ⊘ — it reads the current branch and this session's cwd is seeds while the diff is in bushel. The settings.json swap would otherwise have triggered it on the session 33 precedent, so the specific property was verified by hand instead.

**Gates:** `check-decisions` ✓, `check-context` ✓, `check-docs` ✓ — the doc gate **failed twice for real** during the work (retired skills in two rosters, then a seeds-only path in the synced velocity guide) and both were fixed. `npm run build` ✓. Differ now clean but for a completed one-time migration helper.

**PR:** [bushel PR #294](https://github.com/mobiustripper42/bushel/pull/294)
**Points:** 5
**Branch:** `task/sync-seeds-templates` (in bushel)
**Opened at:** 2026-08-15T00:35:00Z

## Task 2: The differ stops hiding what nobody classified (DEC-S046)

**Completed:**
- **`drift.mjs` now distinguishes "a class I don't diff" from "no class at all"** and reports the second in its own block, printed last and phrased as a seeds-side gap. Reported whether or not the copies differ — the defect is the missing entry, not the bytes.
- **Nine template files had no registry entry** and were therefore never compared in any project. All nine classified: `agents/doc-consistency.md` and `docs/VELOCITY_AND_POKER_GUIDE.md` as `logic`; `agents/pm.md`, `CLAUDE-context.md`, `session-log.md`, `templates/**`, `docs/AGENTS.md`, `docs/CHEATSHEET.md`, `docs/DEV_REFERENCE.md` as `context`.
- **`throughput.py` reclassified `seeds-only`** — `/retro` computes throughput inline from GitHub and never shells out to it. **Third** carve-out from the blanket `scripts/**: logic` glob after `drift.mjs` (PR #172) and `tape-capture.sh` (PR #180); the DEC says a fourth should invert the default instead.
- **`VELOCITY_AND_POKER_GUIDE.md` cited a seeds-only path while being `logic`-class** — copying it verbatim failed a project's `check:docs`. Found by bushel's gate, not by anything here.
- **The shell gains an `npx` rule**: the fleet-wide deny can't be narrowed (same syntax fetches remote packages) and `deny` beats `allow`, so the route is `npm run <script>` or `./node_modules/.bin/<bin>`.
- **DEC-S046** — `refines` DEC-S044 and DEC-S018.

**Proof, and it ran both directions:** a probe file in `dev/claude/` produced exactly one `UNCLASSIFIED` row; removing it produced zero. The original bug was reproduced first — `agents/pm.md` differed in bushel and the differ called bushel clean.

**Code review:** 3 defects, all fixed. **"The only template that names `throughput.py`" was false in two places** — the velocity guide names it too; the conclusion survived but the wording claimed a precision it didn't have. DEC-S046 stated bushel's evidence as bare fact where DEC-S044 explicitly caveats its structurally identical muster/soundings claims; now caveated the same way, with the checkable half named. The shell's `npx` bullet cited DEC-S023 as though it itemized `npx`. Review also prompted `extends` → `refines` on the DEC-S044 edge: S046 never touches the `presence` class, so `extends` overstated it. Everything else the review checked came back clean, including the nine-file accounting one-for-one against the registry and both cited PR numbers.

**PR:** [PR #182](https://github.com/mobiustripper42/seeds/pull/182)
**Points:** 5
**Branch:** `task/drift-unclassified`
**Opened at:** 2026-08-15T01:05:00Z

## Task 3: The capture hook built three filenames out of moving parts

**Completed:**
- **Repo name came from `basename $CWD`**, so a session inside a linked worktree recorded `.sessions-worktree` as the repo. Now from `git rev-parse --path-format=absolute --git-common-dir`. **`--show-toplevel` does not work here** — inside a worktree it returns the worktree path.
- **New `repo_root` field, distinct from `cwd`.** The drain reads the session's `.claude/skills/` for context and a worktree has none — but the path *exists*, so the "directory is gone, skip context" escape hatch never fires and `@tape-reader` gets an empty directory silently.
- **The copy path embedded today's date while the index line is keyed on `session_id`.** A session spanning midnight UTC fired twice, wrote a second file, and left the index pointing at the stale one. **Caught in this session's own transcript** — 650088 bytes indexed, 967248 orphaned. Long sessions are the ones worth auditing, so it preferentially corrupted the best evidence.
- `read-the-tape/SKILL.md` (template + mirror): drain uses `repo_root` with a `cwd` fallback for pre-fix entries, and **reports orphans rather than deleting them** — an orphan is usually the better copy.
- Repaired the live queue by hand: the misfiled muster entry, and the installed hook at `~/.claude/tape-capture.sh`.

**Code review:** 3 findings. **The real one: submodules were the same bug in a second costume, and my fix missed them.** A submodule's common dir is `<super>/.git/modules/<name>` — git bookkeeping that exists, holds no `.claude`, and matched neither case arm. Existing-but-empty is exactly the failure the whole change is about, so shipping the worktree fix while leaving that open would have been the same defect with a different path in it. Reproduced against a constructed submodule, then fixed. Also documented that `GIT_DIR`/`GIT_COMMON_DIR` override `-C` and are **not** defended against — stated so it isn't mistaken for handled.

**Review passes:** `@code-review` ✓ 3 findings. `/security-review` ✓ 0 findings — ran this time because the diff is a new executable firing every session over conversation data, and because cwd and the diff were finally in the same repo. The one candidate (copy path read from `index.jsonl`) needs local write access that already exceeds what it grants.

**Proof:** all four cwd shapes exercised end-to-end through the real hook — submodule, linked worktree, plain checkout, non-repo. None resolve inside `.git/`. Orphan fix proven by firing twice with the copy renamed between fires.

**PR:** [PR #183](https://github.com/mobiustripper42/seeds/pull/183)
**Points:** 3
**Branch:** `task/tape-capture-repo-name`
**Opened at:** 2026-08-16T21:15:00Z

**Next Steps:**
- **After merging PR #183, re-install the hook** — `cp ~/seeds/dev/claude/scripts/tape-capture.sh ~/.claude/tape-capture.sh`. The repo copy is not the one that runs. It was installed once mid-task and the script changed twice after.
- **Then `/read-the-tape --queue` from `~/seeds`.** Still never run. 11 indexed entries now, up from 6 — akaunting, chiplog, muster, seeds, soundings, bushel-mobile. One deliberate orphan remains (`2026-08-16-seeds-3d4ad232-….jsonl`, the complete copy of this session).
- ~~Merge order: PR #182 before bushel PR #294~~ — both merged 2026-08-16.
- **Issue #181** — `check-context.mjs` validates backticked paths but not `§ Section` references, which is why the dead `## Workflow Mechanisms` pointer passed every gate. The cheap half needs no code: a `SCHEMA_VERSIONS.md` note that a project taking the DEC-S042 shell must add that section.
- **A fourth `scripts/**` carve-out should invert the glob**, not add a fourth line. Three have now been corrected after shipping.
- **`/read-the-tape --queue` has still never been run.** Six transcripts sit in `~/.claude/tape-queue/`, and one of them is misfiled: `index.jsonl:1` records `"repo":".sessions-worktree"` for a muster session run inside its worktree. Fix the repo-naming in `tape-capture.sh` before draining, or that observation lands under a repo that doesn't exist.
- **bushel is on hold** — the Wave API can't query data, so the invoicing/reporting work that prompted this session is off. The workflow tooling is current now regardless.
- Open issues: #181 (new), #176 (DEC-S045 shipped in PR #178 — probably closeable), #149, #145, #141.

**Context:**
- **`drift.mjs` reporting clean is not evidence of anything if the registry has holes.** Two of the nine unclassified files had genuinely drifted and the differ said nothing, for months. The check that finds drift was itself the thing hiding it.
- **DEC-S044 and DEC-S046 are the same failure twice.** S044 found `settings.json` invisible-because-unclassified and fixed it by classifying that one file. The mechanism survived and produced two more instances immediately. Fixing an instance is not fixing the mechanism, and the second time costs a second decision record.
- **Permission patterns are project-scoped.** Editing files in bushel from a seeds-anchored session prompts on every single `Edit` — seeds' allowlist doesn't reach another repo. Batch cross-repo edits into one scripted pass with match-count assertions, or work from that repo.
- **`npx` is denied fleet-wide and `deny` beats `allow`**, so no project can allowlist its way out. `./node_modules/.bin/<bin>` works and is verified. A denied command reads as a permission refusal, not an error, so it looks like the agent being difficult rather than the doc being stale.
- **`/security-review` can't reach across repos** — it reads the current branch, and cwd was seeds while the diff was in bushel. A cross-repo task loses that pass silently unless someone says so.
- **The doc gate earned its keep three times this session** — retired skills in two rosters, then a seeds-only path in a synced `logic` file. None of those were in anyone's plan; all three were caught by running the gate rather than by reading the diff.

**Context:**
