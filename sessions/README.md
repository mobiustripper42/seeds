# Sessions branch (DEC-014)

This orphan branch contains all per-session log files. Each Claude session writes one file here, accessed via the worktree at `.sessions-worktree/` from the main checkout.

Do not merge this branch into main. It has zero shared history.
