# seeds — Project Plan

**Start date:** 2026-04-22
**Focus:** Cross-device skill sync via sync-config tooling.
**Critical path:** Upstream Routine + downstream `/pull-seeds` both functional across laptop, headless Ubuntu, and Android.

---

## Estimation Method

Fibonacci scale (2, 3, 5, 8, 13). See `VELOCITY_AND_POKER_GUIDE.md`.
All estimates from planning poker between user and Claude.
No phases — flat task list. Tests baked into each estimate.

**Velocity baseline:** Not yet established.

---

## Tasks

| # | Task | Effort | Status | Notes |
|---|------|--------|--------|-------|
| 1 | De-hardcode skill templates in `dev/claude/skills/` — rewrite the 6 skills (its-alive, pause-this, restart-this, kill-this, its-dead, sync-config) to delegate project-specific commands to `CLAUDE.md §Commands` instead of assuming `npm run build` etc. | 5 | [x] | <!-- completed 2026-04-26 --> Audited all 6 skills. Real changes: kill-this Step 1 (test-suite y/n → verification recap, advisory + non-blocking), kill-this Step 2 + pause-this Step 1 (npm run build → CLAUDE.md §Commands lookup, skip silently if none), its-alive Step 5 ("current phase" → "unchecked tasks"). its-dead, restart-this, sync-config already project-agnostic — no changes needed. 6 file edits across 3 skills × 2 locations. |
| 2 | Update seeds root `CLAUDE.md` setup instructions — step 5 switches from `~/.claude/skills/` (global) to `<project>/.claude/skills/` (project-level). Add Key Docs table pointing at `docs/`. | 2 | [x] | <!-- completed 2026-04-30 --> Code review found 4 more files still referencing old path — fix in follow-up. |
| 3 | Delete `mobile-test` probe skill from seeds `.claude/skills/` | 2 | [x] | <!-- completed 2026-04-25 --> Removed `.claude/skills/mobile-test/` directory. Probe served its purpose. |
| 4 | Research: verify Anthropic Routines can (a) access N private GitHub repos and (b) open a PR to one of them. | 3 | [x] | <!-- completed 2026-04-26 --> Confirmed viable: multi-repo OAuth via /web-setup, PRs to claude/<slug> branches, Pro=5 runs/day. Captured in DEC-010 (the original `DEC-TBD: Anthropic Routines GitHub access model` was retired by DEC-010, not by DEC-006). |
| 5 | Define fixed repo list format for the Routine to scan — where it lives in seeds, what format (JSON / YAML / plain text) | 2 | [x] | <!-- completed 2026-05-07 --> YAML at `.claude/routine-config.yaml` (org list + exclude + directions + per-direction prefixes). Auto-discovery by org scan, not a hand-maintained repo list. Documented in DEC-010. |
| 6 | Build remote Routine for nightly bi-directional sync — invokes `@sync-config` per repo in BOTH directions: upstream (project → seeds, opens stacked backport PR to seeds) and downstream (seeds → project, opens propagation PRs to each lagging project). One nightly run, multiple PRs. The agent is already direction-symmetric (DEC-003) so the orchestration glue is the same regardless of direction — adding the second direction doesn't double the scope. | 8 | [x] | <!-- completed 2026-05-07 --> Routine prompt at `dev/claude/routines/nightly-sync.md` (canonical) + deployment guide at `dev/claude/routines/README.md`. `@sync-config` gained `mode: auto` (non-interactive, applies all backports/forward-ports, defaults-to-skip on ambiguity). PR shape: per (repo × direction), not stacked. Schema-version mismatches → rolling migration backlog issue. Per-run summary → rolling `routine: last run <DATE>` issue. Supersedes DEC-004's upstream-only stance. DEC-010. **Manual deploy step remains:** paste `nightly-sync.md` body into Routine config on claude.ai. |
| 7 | Build `/pull-seeds` downstream skill — manual trigger inside a project, uses `@sync-config` in reverse direction | 5 | [ ] | Depends on 1 (clean skill templates needed). |
| 8 | Decide fate of `scripts/nightly-sync.sh` — supersede with remote Routine, keep as local-only alternative, or retire | 3 | [x] | <!-- completed 2026-05-14 --> Retired. The remote Routine (DEC-010) ran cleanly for ~5 days. `scripts/nightly-sync.sh` removed. Docs/refs in DEC-010 + DEC-TBD updated. |
| 9 | Migrate personal projects to project-level skills — install de-hardcoded skills into each active project's `<project>/.claude/skills/`, verify | 5 | [ ] | Depends on 1. Covers laptop, headless Ubuntu, and at least one repo to test on Android. |
| 10 | Discuss + decide: how the auto-created `claude/<task-slug>` feature branches affect the seeds workflow. | 3 | [x] | <!-- completed 2026-04-24 --> Decided DEC-005: always on main while solo, per-session branches when team grows. |
| 11 | Implement DEC-005 in skills — `/its-alive` Step 0: switch to main + pull before opening session entry. `/its-dead` Step 5: merge non-main working branch into main, delete, push. Touches both `dev/claude/skills/` templates and seeds' own `.claude/skills/` copies (4 files). | 3 | [x] | <!-- completed 2026-04-24 --> Step 0 added to its-alive (4 files), Step 5 added to its-dead (4 files). Takes effect next session — /its-dead Step 5 will auto-clean the current feature branch when Session 5 closes. |
| 12 | Fix DEC-005 implementation bugs surfaced by code review + Session 5 live failure — 6 issues across `/its-alive` Step 0/3 and `/its-dead` Step 5: (1) missing-local-main guard, (2) diverged-main remediation, (3) Step 5 ordering vs commit/push, (4) Step 5 dirty-tree guard, (5) Step 5 remote-delete failure should leave orphan-branch TODO note, (6) `/its-alive` should auto-commit the session-open marker so the stop hook stops firing every session start. | 5 | [x] | <!-- completed 2026-04-25 --> All 6 findings addressed across 4 files. /its-alive Step 0 now handles missing-local-main + diverged-main with user prompt. Step 3 auto-commits + pushes the open marker. /its-dead Step 3 no longer pushes; Step 5 has dirty-tree guard, missing-local-main guard, single push at end, orphan-branch note via commit --amend when remote delete fails. |
| 13 | `/kill-this` push behavior — currently commits but doesn't push, so stop hook fires between `/kill-this` and `/its-dead`. Two options: (a) add `git push origin main` to `/kill-this` Step 3 (2 pushes per session), or (b) live with the hook firing once between halves. Decision + implementation. | 2 | [x] | <!-- completed 2026-04-26 --> Chose (a). /kill-this Step 3 now pushes unconditionally after the commit step (no-op if nothing new). Catches any unpushed earlier commits too. Applied to both kill-this skill files; verified byte-identical. |
| 14 | PR-flow support in `/kill-this` + `/its-dead` — branch detection in kill-this Step 3 (main → DEC-005 push; task/* → gh pr create); PR state detection in its-dead Step 5 (OPEN → push to branch + stop; MERGED → FF-merge cleanup; CLOSED → STOP + ask; no PR → FF-merge cleanup). Fix 2 code review bugs (CLOSED fall-through, literal placeholders). 4 files × 2 skills. | 3 | [x] | <!-- completed 2026-04-30 --> |

| 15 | Build `/read-the-tape` skill + `@tape-reader` agent — 10 known anti-patterns, self-improving candidate discovery, PR flow | 5 | [x] | <!-- completed 2026-05-02 --> Templates in dev/claude/, installed in seeds + sailbook. |
| 16 | Project management workflow rework — per-session files (`sessions/YYYY-MM-DD-HHMM-<dev>-<slug>.md`), `~/.claude/devname` for dev identity, hybrid Issues + PROJECT_PLAN.md model, new `/start-phase` and `/retro` skills, `/read-the-tape` default + Transcript field, dual-mode skill cutover. Installed in bushel as first project on the new model. | 13 | [x] | <!-- completed 2026-05-03 --> 9 SKILL.md files touched in seeds (template + installed), 2 new skills, CLAUDE.md/AGENTS.md updates, bushel `.claude/` + `docs/` + `CLAUDE.md` + DEC-023 (testing) + DEC-024 (PM workflow) installed. |
| 17 | Schema versioning for the seeds workflow — define a version field for PROJECT_PLAN.md / session-file format / skill API; cut a migration path so `/pull-seeds` into older projects (sailbook still on monolithic session-log.md) works without breaking. Pre-req before next `/pull-seeds`. | 5 | [x] | <!-- completed 2026-05-11 --> Shipped Session 15–17 as DEC-006 (`seeds-version` integer at repo root + `<project>/.claude/seeds-version`) and `docs/SCHEMA_VERSIONS.md` (V1/V2/V3 history + migration notes). `/pull-seeds` skill template gates on version match. Row left unchecked through three sessions; housekept Session 22. |
| 18 | Migrate sailbook to new PM workflow — copy seeds template forward, archive `session-log.md`, install per-session files + phase rituals, materialize current sailbook phase as Issues. | 5 | [ ] | Depends on Task 17 (versioning + migration path). |
| 19 | Fix `/kill-this` + `/its-dead` — PR-state check robustness. (a) Hoist the PR state lookup to its own gating sub-step that returns an explicit `STATE` variable; make every downstream branch (OPEN / MERGED / CLOSED / NO_PR) reference `STATE` so silent skip-and-default-to-OPEN can't recur. (b) Add explicit `gh`-unavailable fallback: try MCP github tools (`mcp__github__pull_request_read`, `mcp__github__list_pull_requests`, `mcp__github__create_pull_request`); if neither is present, STOP and ask the user. Never default silently. 4 files: kill-this template + installed, its-dead template + installed. | 2 | [ ] | Surfaced Session 15 — user merged PR#5 between `/kill-this` and `/its-dead`; my `/its-dead` skipped the PR-state check and pushed the session-log finalize commit to the merged-and-orphaned branch instead of main. Required a manual cherry-pick to recover. Same shape of bug applies to `/kill-this` Step 4 PR creation when `gh` is unavailable on web CC. |
| 20 | Project semver workflow — dev projects only (skip seeds + domain). v1.2.3 stored in `package.json` + git tag (skip `VERSION` file). Major = manual via `/bump-major` skill. Minor = bumped by `/retro` on phase close (initial trigger; later `/promote-staging` for projects with staging). Patch = bumped by `/its-dead` on every PR merge to the working branch (or `/ship-it` once that exists). Auto-write CHANGELOG.md entry from PR title on each bump. Build-time `<VersionTag />` component pattern in `dev/claude/` template — reads `process.env.npm_package_version` + `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA`, renders e.g. "v1.2.43 (a1b2c3)" for login-screen / footer display. | 5 | [x] | <!-- completed 2026-05-11 --> Shipped as DEC-007. New `/bump-major` skill in `dev/claude/skills/bump-major/`. `/its-dead` patch-bumps on `STATE=MERGED`; `/retro` minor-bumps at phase close. `dev/claude/templates/VersionTag.tsx` template lands. Housekept Session 22. |
| 21 | Staging-flow conventions in skills — when a project has a `staging` branch, `/kill-this` PRs into `staging` (not `main`); promotion to `main` happens via new `/promote-staging` skill that ff-merges `staging` into `main`, tags the release with the bumped minor version (driven by Task 20), and pushes both. Detect staging vs no-staging via presence of `origin/staging`. Document in `dev/claude/CLAUDE.md §PR Workflow` and seeds `CLAUDE.md`. Add DEC entry recording the ff-merge-not-PR choice (no review benefit when solo, less ceremony). | 3 | [x] | <!-- completed 2026-05-11 --> Shipped as DEC-008. New `/promote-staging` skill in `dev/claude/skills/promote-staging/`. `/kill-this` + `/its-dead` resolve working branch via `git ls-remote --heads origin staging`. Housekept Session 22. |
| 22 | Propagate semver + staging flow into sailbook — run `/pull-seeds` (after Task 20 + 21 land in seeds) to bring `<VersionTag />` template, semver-aware `/its-dead` + `/retro`, and `/promote-staging` skill into sailbook. Wire `<VersionTag />` into the login screen + a footer. Add CHANGELOG.md, seed it from existing v1.0.0 tag forward. Update sailbook `CLAUDE.md` to document the new flow (PRs target staging, promotion via `/promote-staging`). Verify Vercel staging environment is wired (user-side prerequisite). | 3 | [ ] | New. Surfaced Session 16. Depends on Tasks 20, 21, 18 (sailbook V2 migration). The staging branch already exists at `origin/staging` (Session 16). |
| 23 | Supabase prod-write guard — block destructive Supabase CLI ops (`db reset`, `db push --linked`, `db remote commit`, etc.) when the CLI is linked to a production project. Two-layer defense: (a) **discipline:** never `supabase link` to prod locally — production deploys read env vars (`SUPABASE_URL`, service-role key) from Vercel, never from a local `supabase link`; (b) **wrapper script:** `scripts/safe-supabase.sh` calls `supabase status`, parses the linked project ref, and refuses to pass through destructive subcommands if the ref matches a configured prod list (`.claude/prod-supabase-refs` per project, gitignored). Document in `dev/claude/CLAUDE.md §Migration Protocol`. Install template in seeds (`dev/claude/scripts/safe-supabase.sh`); first deploy in sailbook concurrent with staging adoption. | 3 | [x] | <!-- completed 2026-05-11 --> Shipped seeds-side as DEC-009. `dev/claude/scripts/safe-supabase.sh` template lands. Sailbook deployment of the script remains a follow-up (deferred to Task 22 / sailbook V3 pull). Housekept Session 22. |
| 24 | Bushel V2 → V3 migration — first dogfood of the V3 schema. Run from a CC session that has both seeds and bushel checked out. Steps: (1) copy new skill dirs `dev/claude/skills/{bump-major,promote-staging}` from seeds → `<bushel>/.claude/skills/`. (2) copy updated SKILL.md bodies for `its-dead`, `kill-this`, `retro`, `its-alive` (byte-identical from `<seeds>/dev/claude/skills/<name>/SKILL.md`). (3) copy `<seeds>/dev/claude/templates/VersionTag.tsx` → `<bushel>/src/components/VersionTag.tsx`. (4) wire one-time `next.config.js` env forward `NEXT_PUBLIC_APP_VERSION: process.env.npm_package_version` — **critical**, B2 fix; without this the component silently renders `v0.0.0` in client trees. (5) drop `<VersionTag />` into login screen + footer. (6) decide starting version: bushel is fresh → leave `package.json` at `0.1.0`, or set `1.0.0` + tag `v1.0.0` if it feels like a real release. (7) bump `<bushel>/.claude/seeds-version` from `2` to `3`. (8) optional — cut `staging` if the user wants to dogfood that path: `git checkout -b staging main && git push -u origin staging`. (9) verify by running a real session: `/its-alive` → small change → `/kill-this` → merge → `/its-dead` should patch-bump (or minor-bump on staging if (8) was done) and write a CHANGELOG entry. Confirm `<VersionTag />` displays the new version on the deployed login screen. | 3 | [x] | <!-- completed 2026-05-11 --> Bushel confirmed 100% on V3 by user (Session 22). First V3 dogfood is the live verification. Sailbook is now the last V1/V2 holdout — addressed by Task 18. |
| 25 | `/retro` PM commentary — insert Step 4.5 between user verbatim answers (Step 4) and `RETROSPECTIVES.md` append (Step 5) that invokes `@pm` with full phase context (metrics, user answers, session-file paths, closed issues) and captures a 3–5 paragraph commentary covering pace, scope, patterns, and a reaction to the user's answers. User reviews with accept/edit/skip. New section `### PM read` in the RETROSPECTIVES.md template. New 7th responsibility on `@pm`. Tone allowed dry-ironic per `CLAUDE.md §Tone`. Order is post-answers on purpose — pre-answers would prime the user's "what didn't" recall. | 3 | [x] | <!-- completed 2026-05-07 --> Surfaced Session 20 after user noticed bushel's just-completed retro had no PM narrative — was never wired up; original `/retro` skill (introduced 7a699e5) prompted only the three user questions. Folded into PR #11 alongside the Routine work since they're both PM-workflow surfaces. 4 files touched: retro/SKILL.md (template + live), pm.md (template + live). |
| 26 | Routine v2 — form-as-source-of-truth + Provenance labeling + CLAUDE.md scope. Three fixes surfaced by the Routine's first two live runs (2026-05-07 + 2026-05-08): (a) drop the `orgs:` org-scan in `routine-config.yaml` since GitHub OAuth scope is per-repo not org-wide — the Routine form's chip area is the active-set source of truth; (b) per-hunk Provenance labels (`Project-only` / `Template-only` / `Both-modified`) replace the file-level skip the auto-mode classifier defaulted to on first run, so structural template changes propagate even into heavily-customized files like BRAND.md / PROJECT_PLAN.md / CLAUDE.md; (c) add the project root `CLAUDE.md` vs `<seeds>/dev/claude/CLAUDE.md` pair to `@sync-config` Step 1 diff scope — was missing, so structural sections (`§Versioning`, `§Tone`, `§Verbosity`, etc.) had no propagation path. | 3 | [x] | <!-- completed 2026-05-08 --> Files: `.claude/routine-config.yaml` (drop `orgs:`), `dev/claude/routines/nightly-sync.md` (Step 0+1 rewritten), `dev/claude/routines/README.md` (accurate screen-by-screen + form-as-source-of-truth), `CLAUDE.md` (`The Routine` section), `docs/DECISIONS.md` (DEC-010 expanded), `dev/claude/agents/sync-config.md` × 2 (Step 1 scope + Step 2 Provenance + Step 3 table column). |
| 27 | Routine PR-body real-newlines fix — first post-v2 live run (2026-05-08, PR #15) constructed PR body strings with literal `\n` escape sequences instead of actual line break characters; GitHub rendered the four characters `\`, `n`, `\`, `n` as text rather than wrapping the table. PR #15 body had to be hand-reformatted. Add explicit guidance to `nightly-sync.md` (new `## Output formatting` section before Safety guardrails) covering both PR bodies and the rolling `routine: last run` / `routine: migration backlog` issue bodies. Same fix lands the rule next time the prompt is re-pasted to claude.ai. | 1 | [x] | <!-- completed 2026-05-08 --> Single-file change. Verifies on next nightly run. |
| 28 | Project-type gating for template files (DEC-011) — distinguish `webapp` (Next.js + shadcn + Supabase) from `tool` (CLI / agent / library; Node stdlib + shell) so `@sync-config` stops forward-porting irrelevant files (e.g. `agents/ui-reviewer.md` into a tool project). New single-line `<project>/.claude/project-type` file declares the type. New manifest `<seeds>/.claude/type-manifest.yaml` lists which template files are gated to which types. `@sync-config` Step 1 reads both, drops gated file pairs from the diff scope before classification, surfaces one `Type-gated` Provenance row per gated file in Step 3's table. Projects without `.claude/project-type` stay ungated (legacy behavior preserved). Touches: new manifest file, sync-config.md × 2 (Step 1 + Step 3 table), seeds CLAUDE.md (root) repo layout + setup + The Routine sections, dev/claude/CLAUDE.md template Key Docs row, nightly-sync.md PR body template, DEC-011. Backfill `.claude/project-type` in bushel + helm + captains-log + sailbook (in respective project repos, not in seeds). | 3 | [x] | <!-- completed 2026-05-10 --> Shipped Session 21 as DEC-011 via PRs #18 + #19. `.claude/type-manifest.yaml` + new `<project>/.claude/project-type` convention. Bushel, helm, captains-log backfilled. Sailbook backfill folded into Task 18. |
| 29 | Routine duplicate-PR detection — `@sync-config` should check for an open PR already proposing the same diff (either direction) before staging a backport/forward-port commit. Currently the Routine diffs project-main vs seeds-main regardless of pending PRs, so any pending PR's content gets duplicated on the next run from the opposite direction. Concrete fix: in `dev/claude/agents/sync-config.md` Step 1, after computing the file-pair diff, call `mcp__github__list_pull_requests` (state=open) on both seeds and the active project, and skip any file pair whose proposed diff is already on an open PR's HEAD. Surface skipped pairs in Step 6 report as `Already-proposed` with the PR URL. Touches: `dev/claude/agents/sync-config.md` × 2 (template + live, byte-identical), `dev/claude/routines/nightly-sync.md` PR-body template adds `Already-proposed` to the Provenance enumeration. | 3 | [ ] | New. Surfaced Session 21 on 2026-05-11 02:00 EDT run — seeds#21 was opened as a duplicate of the still-open PR #20 (both apply the Glob fix from Task 28+). Cosmetic for now (close-as-duplicate is easy), but at any scale this turns into significant noise. Mitigation: this fix. |
| 30 | Tool-ify `/its-alive` Steps 2 + 4 for validator silence — same pattern as the Glob fix to Step 5 (Task 28's wack-a-mole sibling). Step 2 (dev identity, `cat ~/.claude/devname 2>/dev/null \|\| echo "$USER"`) trips the harness validator's "contains shell syntax (string)" rule because of the `\|\|` operator + `2>/dev/null` redirection. Step 4 (session number, `LEGACY_MAX=$(grep -oE ... \| grep -oE ... \| sort -n \| tail -1) && echo ... && ls sessions/*.md \| wc -l`) trips "contains expansion" because of the command substitution + chained pipeline + glob. Concrete fix: Step 2 uses the **Read** tool on `~/.claude/devname` (with bash `echo "$USER"` fallback only when Read errors); Step 4 uses **Grep** tool on `session-log.md` for the legacy max and **Glob** tool with `pattern: "*.md"` on `sessions/` for the new-format count — then the max-number math happens in Claude's head, no bash pipeline. Mirror to `.claude/skills/its-alive/SKILL.md` (byte-identical). Bushel + helm + captains-log forward-port via the next Routine downstream pass. | 2 | [ ] | New. Surfaced Session 21 on 2026-05-11 after the Glob fix landed — user re-ran `/its-alive` on bushel and Step 5 was silent ✓, but Steps 2 and 4 still prompted. Same class of issue, different commands. Trend is clear: Bash commands with conditionals / expansions / redirects keep tripping validator rules that shift over time. Wherever a skill body can swap a Bash call for a built-in tool (Read / Glob / Grep), the rewrite eliminates the prompt entirely. |
| 31 | DEC-012 — session-end flow rework: `/its-dead` runs BEFORE PR merge, not after. (a) `/its-dead` Step 6 closing summary becomes `STATE`-conditional — `OPEN` says "merge PR #X to ship", `MERGED`/`NO_PR` keep current. Directly fixes the "everything is closed" misreport (S22 → PR #24 sat 36+ hours unmerged). (b) `/its-dead` Step 5.2 `NO_PR` path probes `$WORKING_BRANCH` protection and falls back to PR creation on protected main, instead of failing on 403. (c) `dev/claude/CLAUDE.md` Micro Workflow + PR Workflow + Session Skills table updated — drop `/ship-it` row (never built; folded into /its-dead + manual merge). (d) DEC-012 supersedes DEC-005's direct-push convention for projects with protected `$WORKING_BRANCH`. Touches 3 SKILL.md × 2 (template + live mirror) + dev/claude/CLAUDE.md + docs/DECISIONS.md. | 5 | [x] | <!-- completed 2026-05-12 --> Session 23. Live-verified the gaps on PR #24 (S22 close-out sitting unmerged overnight) and PR #25 (S21 follow-on with orphaned commits). Routine propagates to bushel/captains-log/helm/sailbook tonight (or manual fire). |
| 32 | `/its-alive` Step 0.5 — orphan branch + unmerged PR scan at session start. Two scans: (a) `git for-each-ref refs/remotes/origin/claude/` for branches with commits ahead of `$WORKING_BRANCH`; (b) `gh pr list --state open` (or MCP fallback) for forgotten PRs. Cross-reference into three buckets: Open-with-PR (in flight, advisory), Orphan-without-PR (gating — prompt user to open PR / cherry-pick / delete), Stale-no-commits (quietly suggest cleanup). Surface any PR open >24h as "likely forgotten merge." Skip silently when neither `gh` nor MCP available. Touches `dev/claude/skills/its-alive/SKILL.md` + mirror. | 3 | [x] | <!-- completed 2026-05-12 --> Session 23 alongside Task 31. Caught today by S23's discovery of two stranded branches (`claude/its-alive-task-18-SlS2L` with the S22 close-out + `claude/review-nightly-sync-prs-Ph9Xa` with three follow-on commits). Now session start surfaces these proactively. |
| 33 | Three time fields on session frontmatter — `wall_clock` (raw end−start, auto), `dev_time` (wall_clock minus inferred breaks from transcript JSONL gaps > 15 min, plus user adjustments, auto), `review_time` (PR opened_at → PR merged_at, backfilled by next `/its-alive` Step 7.5). `duration:` stays as synonym for `dev_time:` (legacy velocity-table reader). `/kill-this` Step 4.3 records `pr_number` / `pr_url` / `pr_opened_at` into frontmatter so backfill has its anchor. Unlocks three velocities at `/retro` time — points/wall_clock, points/dev_time, points/review_time. Touches `dev/claude/skills/{its-alive,kill-this,its-dead}/SKILL.md` + mirrors. | 3 | [x] | <!-- completed 2026-05-12 --> Session 23 alongside Task 31. **Superseded by Task 34 (DEC-013/DEC-014)** — Session 24 walked the backfill back. Time math moved to `/retro`; the three frontmatter fields are gone. |
| 34 | DEC-013 + DEC-014 — atomic session files, per-task `/kill-this`, time math at `/retro`, sessions on orphan branch via worktree. (a) `/its-dead` strips all time math + version bumps — just stamps `ended:`, shows wall_clock to screen for gut-check, writes `status: closed`. (b) `/its-alive` Step 7.5 backfill DELETED — atomicity restored. (c) `/kill-this` becomes per-task: appends `## Task <N>` block to the running session file, no time fields, no merge handshake; may run N times per Claude window. (d) `/retro` gains all per-session time math (wall/dev/review from `started`/`ended`/transcript JSONL/GitHub PR timestamps) plus version bumps (patch per merged PR + minor at phase close on dev projects). (e) Session frontmatter slimmed: drop `wall_clock` / `dev_time` / `review_time` / `duration` / `pr_opened_at` / `pr_number` / `pr_url`; replace with `pr_numbers: []` list. Body uses `## Task <N>:` blocks. (f) Sessions move from `sessions/` on `main` to an orphan `sessions` branch checked out at `.sessions-worktree/`. Branch protection on `main` no longer applies to session-file commits. `/its-alive` Step 0.6 ensures the worktree exists; auto-regenerates from `origin/sessions` on a fresh clone. (g) Migration: existing session files copied to orphan branch, `sessions/` removed from main, `.sessions-worktree/` added to `.gitignore`. (h) `docs/WORKFLOW.md` written as a start-to-end webapp dev project workflow reference. Touches: `dev/claude/skills/{its-alive,its-dead,kill-this,retro,pause-this,restart-this}/SKILL.md` × 2 (template + live), `CLAUDE.md` (root) + `dev/claude/CLAUDE.md` (template), `docs/DECISIONS.md`, `docs/PROJECT_PLAN.md`, new `docs/WORKFLOW.md`, `.gitignore`. Bushel mirror is part of the ship. | 13 | [ ] | New. Surfaced Session 24 via dogfooding DEC-012 on bushel — backfill violates session-file atomicity ("session_log needs to be atomic to the claude session"), and the single-/kill-this-per-session assumption doesn't match how stacking actually works (CI waits, task-of-the-moment switches). Workflow user actually wants: one /its-alive, multiple /kill-this (one per PR), one /its-dead. |

**Total: 109 pts** (Sessions 5–17 + 22 + 23 closed; Tasks 7/8/17/18/19/20/21/22/23/24/28/31/32/33 reconciled in Session 22 + 23 + 24 sweeps — full reconciliation at next retro. Task 9 deferred. Tasks 29, 30, 34 still open at the moment of Session 24's writing.)

**Next session priority:** **Dogfood DEC-013 + DEC-014 on bushel** — run a real session end-to-end with the new flow: `/its-alive` (verify Step 0.6 creates the worktree, verify schema), `/kill-this` ×2 on two small changes (verify per-task task blocks land on the sessions branch), `/its-dead` (verify wall_clock displays to screen but nothing time-related lands in the file), merge whenever, eventually `/retro` (verify all time math runs cleanly from session + transcript + GitHub). **Task 29** (Routine duplicate-PR detection, 3 pts). **Task 30** (tool-ify /its-alive Steps 2 + 4, 2 pts — still applies post-rewrite). **Task 9** stays deferred.

---

## Velocity Table

| Work batch | Actual Hours | Effort Points | Hrs/Pt | Notes |
|-----------|-------------|---------------|--------|-------|
| Session 5 (tasks 10, 11) | 0.75 | 6 | 0.125 | Focused work only; 0.5h recovery excluded |
| Session 6 (tasks 12, 3) | 1.00 | 7 | 0.143 | ~1 hr focused |
| Session 7 (tasks 13, 1, 4) | — | 10 | — | Wall clock 8h20m idle-heavy; not representative |

**Lifetime velocity (clean sessions 5+6):** ~0.13 hrs/pt (edit/decision tasks; build tasks likely run higher)

---

## Estimation Poker — Standing Disagreements

| Task | Claude says | You say | Question |
|------|------------|---------|----------|
| — | — | — | — |

---

## Cuttable Tasks (if behind)

| Task | Why it's cuttable | Defer to |
|------|------------------|----------|
| 9 | Can migrate projects opportunistically as they're touched | Rolling |
