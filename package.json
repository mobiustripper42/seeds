{
  "name": "seeds",
  "private": true,
  "description": "Claude Code workflow templates. Deliberately has no `version` field — see the note in CLAUDE.md.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "gen:decisions": "node dev/claude/scripts/gen-decisions-index.mjs",
    "check:decisions": "node dev/claude/scripts/check-decisions.mjs",
    "check:docs": "node dev/claude/scripts/check-docs.mjs",
    "check:mirrors": "node dev/claude/scripts/check-mirrors.mjs",
    "verify": "npm run check:decisions && npm run check:docs && npm run check:mirrors && npm run test"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
